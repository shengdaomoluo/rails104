<div class="col-md-12">
  <div class="group">
    <% if current_user && current_user == @group.user %>
      <%= link_to("Edit", edit_group_path(@group), class: "btn btn-primary pull-right")%>
    <% end %>
  </div>
  <h2 class="text-center"><%= @group.title %></h2>
  <p style="font-size:20px;"><%= @group.description %></p>
</div>
